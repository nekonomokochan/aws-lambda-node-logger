{
  "name": "@nekonomokochan/aws-lambda-node-logger",
  "version": "1.0.2",
  "description": "Logging library for AWS Lambda",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "test": "jest --config=jest.config.json",
    "test:coverage": "jest --config=jest.config.json --collectCoverage=true",
    "tslint": "tslint --project tsconfig.json --config tslint.json 'src/**/*.{ts,tsx}' 'test/**/*.{ts,tsx}'",
    "format": "tslint --project tsconfig.json --config tslint.json --fix 'src/**/*.{ts,tsx}' 'test/**/*.{ts,tsx}'",
    "build": "rm -rf dist && tsc"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nekonomokochan/aws-lambda-node-logger.git"
  },
  "keywords": [
    "lambda",
    "aws",
    "log",
    "logger"
  ],
  "author": "keitakn",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/nekonomokochan/aws-lambda-node-logger/issues"
  },
  "homepage": "https://github.com/nekonomokochan/aws-lambda-node-logger#readme",
  "devDependencies": {
    "@types/jest": "^24.0.13",
    "@types/node": "^12.0.4",
    "coveralls": "^3.0.3",
    "jest": "^24.8.0",
    "prettier": "^1.17.1",
    "ts-jest": "^24.0.2",
    "tslint": "^5.17.0",
    "tslint-config-airbnb": "^5.11.1",
    "tslint-config-prettier": "^1.18.0",
    "tslint-plugin-prettier": "^2.0.1",
    "typescript": "^3.5.1"
  },
  "dependencies": {
    "@slack/client": "^4.12.0"
  }
}
